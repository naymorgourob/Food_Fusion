# Copy this file to .env and fill in real values. .env itself is gitignored.

# Base URL of the backend API (see server/.env.example for the matching PORT).
VITE_API_BASE_URL=http://localhost:5001/api/v1

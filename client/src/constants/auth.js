// Key used in both localStorage (Remember me checked) and sessionStorage
// (unchecked) — see contexts/AuthProvider.jsx for which one is chosen and
// why "Remember me" doesn't need any backend-side concept at all.
export const AUTH_TOKEN_KEY = 'foodfusion-token'

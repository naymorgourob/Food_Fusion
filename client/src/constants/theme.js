// Keys used by ThemeContext to persist and apply the color theme.
// The actual color VALUES live in one place only — src/styles/index.css —
// so there is never a second copy of the palette to fall out of sync.
export const THEME_STORAGE_KEY = 'foodfusion-theme'

export const THEMES = {
  LIGHT: 'light',
  DARK: 'dark',
}

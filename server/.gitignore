node_modules

# Environment variables (see .env.example for the template)
.env
.env.*
!.env.example

# Generated Prisma Client — regenerated by `npm run prisma:generate`,
# never hand-edited, so it doesn't belong in version control.
/src/generated/prisma

# Uploaded files (menu photos, etc. — added when Multer routes are wired up)
/uploads

logs
*.log

